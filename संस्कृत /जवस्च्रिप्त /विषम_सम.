// संस्कृतम्
function प्रतिष्ठापय(पर्यायः) {
    console.log("विषमाः संख्याः अनुक्रमिक्रन्ति:");
    for (let संख्या = 1; संख्या <= पर्यायः; संख्या++) {
        if (संख्या % 2 !== 0) {
            console.log(संख्या);
        }
    }
}

प्रतिष्ठापय(10);
